迪米特法则，也叫做最少知道原则（封装）

一个类对其它的类知道的越少越好
例如电脑关机，需要保存数据，关闭程序，关闭屏幕，断电这几个步骤，而用户关闭电脑并不需要执行以上操作，只需要按下关机键。
把以上四个方法集成在关机方法中，不暴露过多的内部过程。

只和朋友通信
首先，什么是朋友？
类中的属性是朋友，方法的参数是朋友，方法的返回值是朋友，方法中创建的对象是朋友。
可以放心使用朋友的所有方法。
但方法中非我们直接new的对象依旧不是朋友，可以在一个新的方法中处理该对象。（易冗余）

还是将同段代码的抽象等级固定在相同级别上。